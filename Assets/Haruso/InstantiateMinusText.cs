using UnityEngine;

public class InstantiateMinusText : MonoBehaviour
{
    GameObject hyakuSupacha;
    GameObject senSupacha;
    GameObject manSupacha;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
